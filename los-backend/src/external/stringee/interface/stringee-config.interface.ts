// src/profilex/interfaces/profilex-config.interface.ts
export interface StringeeConfig {
  apiKeySecret: string;
  projectId: string;
  apiKeySid: string;
  phoneNumber: string; // Optional, if you want to use a specific phone number
}
